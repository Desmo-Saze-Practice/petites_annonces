{% extends "base.html.twig" %}
{% block body %}
    {% block title %} {{ title }} {% endblock %}
    <h1>{{ title|upper }}</h1>
    <p>{{ date|date('d-m-Y') }}</p>
    <p>{{ dump(date) }}</p>
    <p>{{ content }}</p>
{% endblock %}